---
"@pixel-dsl/core": minor
"@pixel-dsl/lsp": minor
---

Add `formatSource` to core (comment-preserving document format) and wire LSP `documentFormattingProvider` so any LSP client gets format without a dedicated editor extension.
