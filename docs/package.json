{
	"name": "@pixel-dsl/docs",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vitepress dev",
		"prebuild": "bash ../scripts/sync-branding.sh",
		"build": "vitepress build",
		"preview": "vitepress preview",
		"lint": "biome lint --write",
		"format": "biome check --write"
	},
	"devDependencies": {
		"vitepress": "^1.6.0"
	}
}
